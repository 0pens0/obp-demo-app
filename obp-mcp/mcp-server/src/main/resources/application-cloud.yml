spring:
  ai:
    openai:
      api-key: ${vcap.services.llm-service.credentials.api-key:${OPENAI_API_KEY:}}
      chat:
        options:
          model: ${vcap.services.llm-service.credentials.model:gpt-4}
          temperature: 0.7

logging:
  level:
    com.openbanking.mcpserver: INFO
    org.springframework.web: WARN
